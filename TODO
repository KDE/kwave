
o [internal] migrate MP3 import from libmad to libmpg123

o [internal] use libmp3lame natively for MP3 export

o [internal] migrate from libaudiofile to libsndfile

o [internal] support for other (faster) sample rate converters

o [internal] codec_wav: disable workaround for fact chunk == 0 as soon as a newer
  version of libaudiofile is available which can handle this

o [internal] use noise shaping globally in sample2float()

o [internal] more intelligent clipping of position widget

o [internal] debug aid: new plugin for adding a new track with test/debug info:
  - stripe number
  - hull curve
  - test pattern: sample offset within stripe
  - test pattern: stripe borders

o [internal] use xml description for device names in playback/record plugins

o [internal] use some xml file for the menu instead of a macro

o [internal] find a better solution for deadlock when closing the current file while
  a plugin is still running, instead of refusing the request to close

o [internal] more precise handling of the playback pointer
  -> latency handling

o [internal] dithering/noise shaping when loading/saving

o [internal] TrackPixmap: implement usage of "extra_samples" for interpolated mode

o [internal] use a MenuManager in the plugins (e.g. sonagram)

### EOF ###
